// Ask for user's nickname and then capture that
// --> create user in firestore and add to the user's collection along with random code
// --> show connection code and text box to enter connection code
// --> listen for connections and couples
// --> if connection is found, show the connection

// If user is already connected, redirect to Home Page

export default function Connect() {
  return <h1>Connect</h1>;
}
