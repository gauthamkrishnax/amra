#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

branch=$(git rev-parse --abbrev-ref HEAD)

if [ "$branch" = "master" ] || [ "$branch" = "main" ]; then
  echo "❌ Direct push to $branch is blocked. Create a feature branch and open a PR."
  exit 1
fi
